#include<bits/stdc++.h>
using namespace std;
typedef long long int  ll;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout << "Insert number of elements and total amount  ";
    int n, amount;
    cin>>n>> amount;
   // cout << "Insert elements of the array: \n";

    return 0;
}

